version: '2'

services:
  logaggregator:
    container_name: logaggregator
    image: petprojects/log-aggregator:${BUILD_VERSION}
    network_mode: bridge
    build:
      context: ./src/Presentation.ConsoleApplication
      dockerfile: Dockerfile
      args:
        source: ${OUTPUT_PATH}
